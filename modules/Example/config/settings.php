<?php

declare(strict_types=1);

/*
| Module config — merged under config('module.example').
| Read at runtime via module_setting('example', 'key') which checks the
| module_settings DB table first, then falls back to these defaults.
*/
return [
    'per_page' => 15,
];
