includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    paths:
        - app
        - modules

rules:
    # Typo-safe hooks: forbid string-literal hook names (must use HookConstants).
    - App\Tools\PHPStan\HookConstantRule
